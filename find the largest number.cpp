#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    int n,m;
    cout<<"Enter two integers numbers: ";
    cin>>n>>m;
    if(n>m)
        cout<<"The largest number is " <<n;
    else
        cout<<"The largest number is "<<m;
}
